[phases.setup]
nixPkgs = ['nodejs-18_x']

[phases.install]
cmds = ['npm ci']

[phases.build]
cmds = ['echo "Build complete - frontend copied via postinstall"']

[start]
cmd = 'NODE_ENV=production node server.js'

