{
  "name": "ujikom-web-event",
  "version": "1.0.0",
  "scripts": {
    "build": "cd frontend && npm ci && npm run build"
  },
  "dependencies": {
    "mysql2": "^3.14.5",
    "node-cron": "^4.2.1",
    "xlsx": "^0.18.5"
  }
}
